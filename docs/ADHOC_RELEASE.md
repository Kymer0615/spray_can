This release is **ad-hoc signed and not notarized by Apple**. If macOS blocks first launch, use System Settings → Privacy & Security → Open Anyway for the app you choose to trust. Enable Accessibility for navigation; Screen Recording is optional for on-device OCR.

Install through Homebrew:

```sh
brew install --cask kymer0615/tap/spray-can
```

Includes native Liquid Glass labels, customizable colors, adaptive placement for crowded controls, and private on-device text recognition. Compatibility varies across applications; see the README and docs/VALIDATION.md for verified results and remaining checks.
